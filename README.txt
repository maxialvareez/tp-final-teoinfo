- Para ejecutar .jar:

1.- Abrir consola y posicionarse en la carpeta "dist" del proyecto.
2.- Ejecutar teoo.jar mediante el comando java -jar "teoo.jar"

La carpeta "ArchivosCatedra" ya se encuentra en la ruta requerida.

--- o ---

Se dejo duplicada la carpeta "ArchivosCatedra" en el directorio inicial
del proyecto por si se quiere abrir en un IDE, luego de "Clean and Build" se borrara automáticamente
dicha carpeta en el directorio dist, por lo que se debe copiar y pegar nuevamente desde el directorio
"Proyecto TPE Teo" a "\Proyecto TPE Teo\dist"

Si se ejecuta en PowerShell puede llegar a cortar datos, se recomienda ejecutar en cmd.

La carpeta "Archivos Comprimidos" se genera automaticamente dentro de "Proyecto Tpe Teo"
